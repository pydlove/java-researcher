<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiocloud.test.license.mapper.LicenseManagementMapper">

    <update id="updateUsedLicenseWithCheck">
        UPDATE license_management
        SET used_license = used_license + #{delta},
            version = version + 1,
            update_time = NOW()
        WHERE id = #{id}
          AND version = #{version}
          AND (total_license - used_license) >= #{delta}
    </update>

    <select id="selectWithLock" resultType="com.aiocloud.test.license.po.LicenseManagement">
        SELECT id, total_license as totalLicense, used_license as usedLicense,
        version, create_time as createTime, update_time as updateTime
        FROM license_management
        WHERE id = #{id}
        <if test="lock">
            FOR UPDATE
        </if>
    </select>
</mapper>